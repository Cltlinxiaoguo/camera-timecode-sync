# 相机同步检测工具 — 测试用例与自动化

> 本文档与 [相机同步检测工具_需求与设计.md](./相机同步检测工具_需求与设计.md) 配套使用，对其第 7 节进行展开。

---

## 1. 测试范围与原则

- **快速默认**：日常自动化（`run_tests.bat`）以 mock 替代真实 PaddleOCR / rawpy / CR2 文件，避免下载模型与依赖相机素材，单次执行 < 10 秒。
- **可拆函数**：时间码解析、是否同步判定、Excel 汇总行 / 条件格式规则、配置解析、目录清理等均**脱离硬件**单测。
- **慢测可选**：真实 OCR / 真实 CR2 全链路属 `pytest -m slow`，默认不参与 CI；现场验收阶段手工触发。
- **覆盖目标**：核心模块（timecode / excel_writer / config / copy_false / clear_folder / pipeline）行覆盖 ≥ 85%。

---

## 2. 测试金字塔

| 层级 | 关注 | 框架 / 工具 | 默认是否运行 |
|------|------|-------------|--------------|
| 单元测试 | 纯函数 / 单模块 | pytest + mock | ✅ |
| 模块集成 | DataFrame → xlsx，Excel → copy 等真磁盘 | pytest + 临时目录 | ✅ |
| Pipeline smoke | `pipeline.run()` 关掉 CR2/OCR 真实步骤 | pytest + monkeypatch | ✅ |
| 端到端验收 | 双击 exe 全流程 | 人工 | 仅 release / 验收阶段 |

---

## 3. 测试用例总表

> 列含：用例ID、用例名称、测试类型、前置/输入、操作步骤、预期结果、实际结果、备注。  
> 可整表复制至 Excel 作为联调记录，**实际结果**列在执行后填入。

### 3.1 全量用例

| 用例ID | 用例名称 | 测试类型 | 前置/输入 | 操作步骤 | 预期结果 | 实际结果 | 备注 |
|--------|----------|----------|-----------|----------|----------|----------|------|
| TC-TIMECODE-001 | 仅提取符合正则的时间码 | 单元 | OCR 输出 4 条文本，含 2 条 `HH:MM:SS:FF`、1 条普通文本、1 条短格式 | 调用 `extract_timecodes` | 仅返回 2 条匹配项 |  | 严格 fullmatch |
| TC-TIMECODE-002 | 输入为空时返回空列表 | 单元 | `[]` 与 `None` | 调用 `extract_timecodes` | 返回 `[]` |  | |
| TC-TIMECODE-003 | 跳过缺字段 / 残缺条目 | 单元 | 包含 `None`、`[]`、缺 score 的行 | 调用 `extract_timecodes` | 仅取得格式正确的时间码 |  | |
| TC-TIMECODE-004 | fullmatch 不能匹配子串 | 单元 | 文本 `"prefix 01:02:03:04 suffix"` | 调用 `extract_timecodes` | 返回 `[]` |  | 防止 OCR 把行号误识为时间码 |
| TC-TIMECODE-005 | 首段（小时位）强制归零 | 单元 | `["12:34:56:78", "00:00:00:00"]` | 调用 `force_hour_zero` | `["00:34:56:78", "00:00:00:00"]` |  | 与原脚本一致 |
| TC-TIMECODE-006 | 非 4 段字符串保持不变 | 单元 | `["12:34:56", "abc"]` | 调用 `force_hour_zero` | 输入原样返回 |  | |
| TC-TIMECODE-007 | 同步判定全相同 → True | 单元 | `["00:01:02:03"]*2` | 调用 `is_same_timecodes` | `True` |  | |
| TC-TIMECODE-008 | 同步判定空 → False | 单元 | `[]` | 调用 `is_same_timecodes` | `False` |  | 与原脚本 `bool(timecodes)` 一致 |
| TC-TIMECODE-009 | 同步判定不一致 → False | 单元 | `["00:01:02:03","00:01:02:04"]` | 调用 `is_same_timecodes` | `False` |  | |
| TC-CONFIG-001 | 加载最小可用 YAML | 单元 | conftest 提供的 `minimal_yaml` | 调用 `load_config` | 返回 `AppConfig`，字段值与 YAML 一致 |  | |
| TC-CONFIG-002 | YAML 不存在自动写默认模板 | 单元 | `tmp_path/camera_sync_config.yaml` 不存在 | 调用 `load_config(tmp_path/...)` | 文件被创建并加载成功 |  | 默认模板保留与原脚本一致的取值 |
| TC-CONFIG-003 | 非法颜色值抛 ConfigError | 单元 | `excel.highlight_color: 'ZZZZZZ'` | 调用 `load_config` | 抛 `ConfigError` |  | |
| TC-CONFIG-004 | 非法 crop 抛 ConfigError | 单元 | `crop.x: -1` | 调用 `load_config` | 抛 `ConfigError` |  | 边界值校验 |
| TC-CONFIG-005 | 默认模板字符串结构完整 | 单元 | 直接读取 `DEFAULT_CONFIG_TEMPLATE` | 字符串包含 7 大节 | `paths/crop/ocr/runtime/excel/pipeline/ui` 全部存在 |  | |
| TC-CONFIG-006 | 默认模板可被加载并通过校验 | 单元 | 用模板写文件后 `load_config` | 校验通过；crop 默认 `1621x1428` |  | 与 `Cr2 Cut Into Jpg.py` 一致 |
| TC-CONFIG-007 | 相对路径以 YAML 目录为基准解析 | 单元 | `cr2_dir: 'cr2'` | 加载 | 解析为 `<yaml_dir>/cr2` 绝对路径 |  | |
| TC-CLEAR-001 | 清空内含文件的目录 | 单元 | 目录内 2 文件 | `clear_folder_contents` | 返回 2，目录为空 |  | |
| TC-CLEAR-002 | 清空内含子目录的目录 | 单元 | 子目录 + 文件 | `clear_folder_contents` | 全部移除 |  | 递归 rmtree |
| TC-CLEAR-003 | 目录不存在自动创建 | 单元 | `ensure_exists=True` | 调用 | 目录被创建 |  | |
| TC-CLEAR-004 | 目录不存在但不创建 | 单元 | `ensure_exists=False` | 调用 | 不创建，返回 0 |  | |
| TC-CLEAR-005 | 路径是文件 → NotADirectoryError | 单元 | 传入文件路径 | 调用 | 抛 `NotADirectoryError` |  | |
| TC-EXCEL-001 | DataFrame 按文件名升序 + Is Same | 单元 | 3 张图，含同步/不同步/空 | `build_dataframe` | 按 a/b/c 排序，IsSame 列符合预期 |  | |
| TC-EXCEL-002 | Summary 文案与汇总数 | 单元 | 4 条数据 | `build_summary_row` | 含 `Sync Rate: 50.00%` |  | |
| TC-EXCEL-003 | Excel 文件名带时间戳 | 单元 | 注入固定时间 | `make_report_path` | `Sync_n_Timecode_YYYYMMDD_HHMMSS.xlsx` |  | |
| TC-EXCEL-004 | **修复 Bug**：False 行高亮含最后一行 | 集成 | 4 条数据 + Summary，最后一行 IsSame=False | `write_report` | 4 条 False 数据全部高亮，Summary 不高亮 |  | 原脚本 `max_row=len(df)-1` 漏最后一行 |
| TC-EXCEL-005 | 饼图被插入工作表 | 集成 | 任意结果 | `write_report(insert_chart=True)` | `ws._images` ≥ 1 |  | |
| TC-COPY-001 | 仅复制 IsSame=False 的行 | 集成 | 3 张磁盘 jpg + xlsx | `copy_false_images` | 复制 2 个不同步图，True 项不复制 |  | |
| TC-COPY-002 | 源缺失时打印明确日志 | 集成 | xlsx 中含磁盘缺失文件 | `copy_false_images` | 日志含 `File not found` |  | PRD P5 |
| TC-COPY-003 | Summary 行不会被误复制 | 集成 | 仅 1 行 True 数据 | `copy_false_images` | 复制 0 文件，false 目录为空 |  | |
| TC-LOG-001 | 控制台与文件双输出 + UTF-8 | 单元 | 任意临时目录 | `setup_logging` 后 `info('中文')` | 文件包含中文 |  | |
| TC-LOG-002 | 重复初始化幂等 | 单元 | 连续两次 `setup_logging` | 检查 handlers 数量 | 不会爆炸式累加 |  | |
| TC-PIPELINE-001 | 端到端 smoke（mock OCR） | 集成 | minimal yaml + 假 jpg | `pipeline.run` | 退出码 0，xlsx 落盘，false 目录正确归档 |  | |
| TC-PIPELINE-002 | OCR 无输出时退出码 4 | 集成 | mock 返回 None | `pipeline.run` | 退出码 4 |  | |
| TC-PIPELINE-003 | 配置错误退出码 2 | 集成 | YAML 顶层非 dict | `pipeline.run` | 退出码 2，不抛异常 |  | 防止"无声崩溃" |
| TC-SLOW-001 | 真实 PaddleOCR 单图识别 | 慢速 | 1 张含 `00:01:02:03` 的图 | 调用 `process_image`（真实 init） | 识别出该时间码 |  | `pytest -m slow`，CI 默认不跑 |
| TC-SLOW-002 | 真实 rawpy 转 JPG | 慢速 | 1 张 CR2 | `convert_one` | 输出 JPG，尺寸 = crop 尺寸 |  | |
| TC-ACC-001 | 双击 exe 全流程跑通（人工） | 验收 | 完整 CR2 数据集 + 配置文件 | 双击 `相机同步检测工具.exe` | xlsx + false 目录 + 控制台进度 + 按回车退出 |  | PRD 验收行 |
| TC-ACC-002 | exe 同目录 YAML 修改即时生效 | 验收 | 改 `crop.x = 0` | 重跑 | 输出 JPG 起点改变 |  | PRD P3 |
| TC-ACC-003 | YAML 缺失启动写默认模板 | 验收 | 删除 YAML 后双击 | 启动 | exe 同目录生成默认 YAML 后正常运行 |  | |
| TC-ACC-004 | 控制台关闭前可读完日志 | 验收 | 任意运行 | 看到"按回车键退出"提示 | 提示存在；按回车后退出 |  | PRD P5 |

### 3.2 PRD 优先级映射

| PRD | 关联用例 |
|-----|----------|
| **P1 — 基础流程** | TC-CLEAR-001~005, TC-PIPELINE-001~003, TC-SLOW-002, TC-ACC-001 |
| **P2 — 代码优化（配置外置 / 健壮 / 性能）** | TC-CONFIG-001~007, TC-PIPELINE-003, TC-CLEAR-003, ProcessPoolExecutor `initializer` 设计验证（TC-SLOW-001 隐含） |
| **P3 — 配置文件分离** | TC-CONFIG-002 / TC-ACC-002 / TC-ACC-003 |
| **P4 — 打包与交付** | TC-ACC-001 / TC-ACC-004（人工验收）|
| **P5 — 细节体验** | TC-ACC-004（按回车退出）/ TC-EXCEL-003（时间戳）/ TC-COPY-002（明确日志）/ TC-LOG-001（UTF-8）|

---

## 4. mock 说明

| 真实依赖 | mock 方式 | 出现位置 |
|----------|-----------|----------|
| `PaddleOCR` | `pipeline_mod.run_ocr_and_report` 被 monkeypatch 整体替换；不进入 `ocr_worker._init_worker` | `tests/test_pipeline_smoke.py` |
| `rawpy` | 关闭 `pipeline.do_cr2_to_jpg` 子步骤；测试集中精力验证 OCR 后逻辑 | `tests/test_pipeline_smoke.py` |
| 真实 CR2 文件 | 直接在 `jpg_dir` 放零字节 jpg；OCR 步骤已被 mock | `tests/test_pipeline_smoke.py` |
| `cv2.imread` | 当前不直接 mock；如需测试 ocr_worker 真实路径请打 `slow` 标记 | — |
| `matplotlib` | `excel_writer._draw_pie_chart` 使用 `Agg` 后端，无 GUI 即可执行 | 自动 |

---

## 5. 一键执行

```bat
# Windows 一键（默认排除慢测）
run_tests.bat
```

```bat
# 包含慢测（需要安装 paddleocr / paddlepaddle / rawpy 与示例数据）
python -m pytest -q -m "slow"
```

```bat
# 覆盖率报告
python -m pytest -q -m "not slow" --cov=camera_sync --cov-report=term-missing
```

---

## 6. 交付清单

- ✅ `tests/` 目录，含 `conftest.py` 与 6 个测试文件
- ✅ `pytest.ini`（含 `slow` / `integration` markers）
- ✅ `run_tests.bat`（Windows UTF-8 一键）
- ✅ `requirements-dev.txt`（pytest / pytest-cov / freezegun / pyinstaller）
- ✅ 本表（用例 ID/名称/类型/输入/步骤/预期/备注）

---

*文档版本：与「相机同步检测 exe」开发计划同步维护。*
